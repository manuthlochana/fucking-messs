# Core engine
crawl4ai>=0.5.0
google-genai>=1.0.0
pydantic>=2.6
aiohttp>=3.9.0          # Phase 2/3/4 HTTP calls in forensic pipeline

# Database
asyncpg>=0.29.0          # async PostgreSQL driver
pgvector>=0.2.5          # pgvector Python codec (optional, for embedding inserts)

# LLM pool fallback
groq>=0.9.0              # Groq API client (optional, set GROQ_API_KEY to enable)

# Optional conveniences
python-dotenv>=1.0.0      # load .env automatically
redis>=5.0.0             # only needed if you set REDIS_URL for the hash ring

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
anyio>=4.0.0
